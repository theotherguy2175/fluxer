[package]
name = "fluxer_win_toast"
version = "0.0.0"
edition = "2024"
license = "AGPL-3.0-or-later"
publish = false

[workspace]
resolver = "2"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
napi = {version = "3.12.6", default-features = false, features = ["dyn-symbols", "napi8", "serde-json"]}
napi-derive = "3.6.7"
serde = {version = "1.0", features = ["derive"]}

[target.'cfg(target_os = "windows")'.dependencies]
windows = {version = "0.62.2", features = [
	"Data_Xml_Dom",
	"Foundation",
	"UI_Notifications",
	"Win32_System_WinRT",
]}

[build-dependencies]
napi-build = "2.4.3"

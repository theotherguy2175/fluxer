[package]
name = "fluxer_mac_sysctl"
version = "0.0.0"
edition = "2024"
license = "AGPL-3.0-or-later"
publish = false

[workspace]
resolver = "2"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
napi = {version = "3.12.6", default-features = false, features = ["dyn-symbols", "napi8", "tokio_rt"]}
napi-derive = "3.6.7"
sysctl = "0.7.1"
tokio = {version = "1.53.1", features = ["rt"]}

[build-dependencies]
napi-build = "2.4.3"
